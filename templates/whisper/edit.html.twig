{% extends 'base.html.twig' %}

{% block title %}Modification d'un Audio{% endblock %}

{% block body %}
    <h1>Modification d'un Audio</h1>

    {{ include('whisper/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_whisper_index') }}" class="btn btn-primary">Retour à la liste</a>

    {{ include('whisper/_delete_form.html.twig') }}
{% endblock %}